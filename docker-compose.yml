services:
  jekyll:
    image: jekyll/jekyll:4
    working_dir: /srv/jekyll
    command: jekyll serve --livereload --host 0.0.0.0 --port 4000 --force_polling
    ports:
      - "4000:4000"
      - "35729:35729"
    volumes:
      - .:/srv/jekyll
